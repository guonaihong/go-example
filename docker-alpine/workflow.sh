#!/bin/sh

case "$1" in
    "build" )
        echo $1
        ;;

    "reset.docker" )
        echo $1
        ;;

    "build.docker" )
        echo $1
        ;;

    "run.docker" )
        echo $1
        ;;

esac

